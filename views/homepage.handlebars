
{{#each posts as | post| }}

<div class="row mb-4 project">
    <h2 class="text-uppercase">
      <a href="/blogpost/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.description}}
    </p>
  <div>created by {{post.user.username}} on {{format_date date_created}}</div>
</div>
{{/each}}